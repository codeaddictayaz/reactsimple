import { createTheme } from "@mui/material";
export const theme = createTheme({
  palette: {
    primary: {
      main: "#ff3333",
    },
    secondary: {
      main: "#33ff33",
    },
    myColors: {
      main: "#3333ff",
      light: "#5c5cff",
      dark: "#1f1f99",
    },
  },
});
